import 'package:flutter/material.dart';

class FormUno extends StatelessWidget {
  const FormUno({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}